#include <stdio.h>
int main(){
    int a , b, c;
    printf('moi nha so a: ');
    scanf('%d',a);
    printf('moi nha so b: ');
    scanf('%d',b);
    printf('moi nha so c: ');
    scanf('%d',c);
    if (a + b + b == 0) {
        printf('so do thoa man dinh thuc a^3 +b^3 + c^3 = 3abc');
    }
    else{
        printf('so do khong thoa man');
    }
}